
lab1=#include <stdio.h>
#include<stdlib.h>
#include<string.h>
struct calElement
{
    char *day;
    int date;
    char *activity;
};
struct calElement*create()
{
    struct calElement*calendar;
    calendar=(struct calElement*)malloc(sizeof(struct calElement)*7);
    return calendar;
}
void read(struct calElement*calendar)
{
    char day[10];
    char activity[25];
    int i,date;
    for(i=0;i<7;i++)
    {
        printf("Enter the day:");
        scanf("%s",day);
        calendar[i].day=(char*)malloc(strlen(day)+1);
        strcpy(calendar[i].day,day);
        printf("Enter the date:");
        scanf("%d",&date);
        calendar[i].date=date;
        getchar();
        printf("Enter description of the activity:");
        scanf("%[^\n]s",activity);
        calendar[i].activity=(char*)malloc(strlen(activity)+1);
        strcpy(calendar[i].activity,activity);
    }
}
void display(struct calElement*calendar)
{
    int i;
    printf("\n\nYour calendar\n");
    printf("Day\t\tDate\t\tActivity");
    for(i=0;i<7;i++)
    {
        printf("\n%s\t\t%d\t\t%s",calendar[i].day,calendar[i].date,calendar[i].activity);
    }
}
void main()
{
    struct calElement*calendar;
    calendar=create();
    read(calendar);
    display(calendar);
    free(calendar);
}
lab2=
#include <stdio.h>
char STR[100],PAT[100],REP[100],ANS[100];
int s,p,r,a,flag=0;
void main()
{
    read();
    replace();
    display();
}
void read()
{
    printf("Enter the MAIN string\n");
    scanf("%s",STR);
    printf("Enter the PATTERN string\n");
    scanf("%s",PAT);
    printf("Enter the REPLACE string\n");
    scanf("%s",REP);
}
void replace()
{
    s=p=a=0;
    while(STR[s]!='\0')
    {
        if(STR[s]==PAT[p])
        {
            p++;
            s++;
            if(PAT[p]=='\0')
            {
                flag=1;
                for(r=0;REP[r]!='\0';r++,a++)
                    ANS[a]=REP[r];
                p=0;
            }
        }
        else
        {
            ANS[a]=STR[s];
            s++;
            a++;
            p=0;
        }
    }
}
void display()
{
    if(flag==0)
       printf("Pattern doesn't found!!!");
    else
    {
        ANS[a]='\0';
        printf("\nThe RESULTANT string is:\n%s\n",ANS);
    }
}
mkdir Git; cd Git
git init
touch Anita.c
vi Anita.c
git add .
git commit
//user email and name//
git status
vi Anita.c
git add .
git commit -m "first change"
git log

2=
git branch feature-branch
git branch
vi.Anita.c
git add .
git commit -m "second change"
git checkout feature -branch
vi Anita.c
git add .
git commit -m "third change"
git checkout master 
git merge feature-branch
vi Anita.c
git add .
git commit -m "fourth change"
git log

3=
git checkout feature -branch
vi Anita.c
git stash
git stash apply
git status
git add .
git commit -m "stash change"
git log

4=
git clone//code
cd // half code

5=
git fetch
git config --global pull.rebase "TRUE"
git pull

6=
cd Git
Is
vi git.c
git add
git commit -m "new file"
git checkout master
vi git.c
git add .
git commit -m "change"
git log

7=
git tag -a v1 -m "first change"
git show v1

8=
git branch //s
git checkout //s
vi git c
git add .
git commit
git log
git checkout master
git cherry-pick --about //code
git show //copy

9=
git show

10=
git log
git log --author="Anita" --since="//code//"  --until=""

11=
git log -5

12=
git revert//code
